{
    "configurations": [
        {
            "type": "pwa-chrome",
            "request": "launch",
            "name": "Launch!",
            "url": "http://localhost/feedo",
            "webRoot": "${workspaceFolder}"
        }
    ]
}
